---
import { formatDate } from "../../lib/helpers";

interface Props {
  title: string;
  date: Date;
  id: string;
}

const { title, date, id } = Astro.props;
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
---

<li
  class="list-none w-full text-neutral-900 hover:text-neutral-500 ease-in-out transition-colors border-b-neutral-400 border-dashed border-b-1 my-2"
>
  <a
    class="text-sm md:text-base decoration-none flex justify-between"
    href={`${base}/blog/${id}`}
  >
    <p class="inline-block whitespace-nowrap">
      {title}
    </p>
    <time
      class="text-right tabular-nums"
      data-date={date.toISOString()}       datetime={date.toISOString()}
    >
      {formatDate(date)}
    </time>
  </a>
</li>
